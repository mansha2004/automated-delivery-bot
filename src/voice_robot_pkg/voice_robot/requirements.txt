# ── Core (always needed) ──────────────────────
rapidfuzz
networkx
pyttsx3
pyaudio      # needed by SpeechRecognition for mic input

# ── STT backends (pick one) ───────────────────
SpeechRecognition   # Google STT fallback

# Moonshine (recommended – offline, fast, ~100 MB)
moonshine-onnx
sounddevice

# Vosk (alternative offline STT)
# vosk>=0.3.45

# ── Embedding match (optional) ────────────────
# sentence-transformers>=2.7
# scikit-learn>=1.4
